# Evaluation Report

This file is intentionally reserved for **measured results from the executed capstone notebook**.

The capstone rubric requires the report to be based on the real application pipeline. Do not replace this statement with claimed results until the evaluation cells have actually run.

The final report must contain:

- overall results for both backends;
- results sliced by intent, language, difficulty, and risk;
- structured-output validation pass rate by language;
- deterministic safety results with safety stratum = 100%;
- attack block rate and legitimate false-positive rate;
- judge calibration evidence and Cohen's kappa;
- clean regression-gate run;
- seeded-regression blocked run;
- known limitations.

The notebook outputs are the source of truth.
